4)SELECT FIRST_NAME AS WORKER_NAME 
 FROM WORKER;

5)SELECT UPPER(FIRST_NAME)
 FROM WORKER;

6)SELECT DISTINCT DEPARTMENT
 FROM WORKER;

7)SELECT INSTR(FIRST_NAME,'A') 
FROM WORKER;

8)SELECT SUBSTR(FIRST_NAME,1,3) 
FROM WORKER;

9)SELECT RTRIM(FIRST_NAME)
FROM WORKER;

10)SELECT LTRIM(FIRST_NAME)
FROM WORKER;

11)SELECT DISTINCT DEPARTMENT,LENGTH(DEPARTMENT)
FROM WORKER;

12)SELECT REPLACE(FIRST_NAME,'A','a')
FROM WORKER;

13)SELECT FIRST_NAME||' '||LAST_NAME AS COMPLETE_NAME
FROM WORKER;

14)SELECT * 
FROM WORKER
ORDER BY FIRST_NAME;

15)SELECT * 
FROM WORKER
ORDER BY FIRST_NAME, DEPARTMENT DESC;

16)SELECT *
FROM WORKER
WHERE FIRST_NAME IN ('Vipul','Satish');

17)SELECT *
FROM WORKER
WHERE FIRST_NAME NOT IN ('Vipul','Satish');

18)SELECT *
FROM WORKER
WHERE DEPARTMENT='Admin';

19)SELECT * 
FROM WORKER
WHERE FIRST_NAME LIKE '%A%';

20)SELECT * 
FROM WORKER
WHERE FIRST_NAME LIKE '%A';

21)SELECT * 
FROM WORKER
WHERE FIRST_NAME LIKE '%H' 
AND LENGTH(FIRST_NAME)=6;

22)SET SERVEROUTPUT ON;
DECLARE
 V_MAX_DEPT_ID DEPARTMENTS.DEPARTMENT_ID%TYPE;
BEGIN
 SELECT MAX(DEPARTMENT_ID) INTO V_MAX_DEPT_ID
 FROM DEPARTMENTS;
 DBMS_OUTPUT.PUT_LINE('MAX DEPT_ID '||V_MAX_DEPT_ID);
END;

23)SET SERVEROUTPUT ON;
DECLARE
 "SELECT" DEPARTMENTS.DEPARTMENT_ID%TYPE;
BEGIN
 SELECT MAX(DEPARTMENT_ID) INTO "SELECT"
 FROM DEPARTMENTS;
 DBMS_OUTPUT.PUT_LINE('MAX DEPT_ID '||"SELECT");
END;

24)DECLARE 
 V_VAR1 VARCHAR2(10):='GLOBAL1';
 V_VAR2 VARCHAR2(10):='GLOBAL2';
BEGIN
 DECLARE
  V_VAR1 VARCHAR2(10):='LOCAL1';
  V_VAR2 VARCHAR2(10):='LOCAL2';
 BEGIN
  DBMS_OUTPUT.PUT_LINE('NESTED OUTPUT: '||V_VAR1);
  DBMS_OUTPUT.PUT_LINE('NESTED OUTPUT: '||V_VAR2);
 END;
 DBMS_OUTPUT.PUT_LINE('MAIN OUTPUT: '||V_VAR1);
 DBMS_OUTPUT.PUT_LINE('MAIN OUTPUT: '||V_VAR2);
END;

25)DECLARE
 V_UP_SAL NUMBER(10);
BEGIN
 SELECT SALARY+10000 INTO V_UP_SAL
 FROM EMPLOYEES
 WHERE EMPLOYEE_ID=122;
 DBMS_OUTPUT.PUT_LINE('UPDATED SALARY: '||V_UP_SAL);
END;