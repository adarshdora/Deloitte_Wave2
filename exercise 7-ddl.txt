1)CREATE TABLE DEPT(ID NUMBER(7) NOT NULL PRIMARY KEY, NAME VARCHAR2(25));

2)INSERT INTO DEPT(ID, NAME)
SELECT DEPARTMENT_ID, DEPARTMENT_NAME
FROM DEPARTMENTS;

3)CREATE TABLE EMP(ID NUMBER(7), LAST_NAME VARCHAR2(25), FIRST_NAME VARCHAR2(25), DEPT_ID NUMBER(7), FOREIGN KEY(DEPT_ID) REFERENCES  DEPT(ID));

4)CREATE TABLE EMPLOYEES2(ID, FIRST_NAME, LAST_NAME, SALARY, DEPT_ID)
AS (SELECT EMPLOYEE_ID AS ID, FIRST_NAME AS FIRST_NAME, LAST_NAME AS LAST_NAME, SALARY AS SALARY, DEPARTMENT_ID AS DEPT_ID 
FROM EMPLOYEES);

5)ALTER TABLE EMPLOYEES2 READ ONLY;

6)INSERT INTO EMPLOYEES2(SELECT EMPLOYEE_ID, FIRST_NAME, LAST_NAME, SALARY, DEPARTMENT_ID FROM EMPLOYEES);

7)ALTER TABLE EMPLOYEES2 READ WRITE;

INSERT INTO EMPLOYEES2(SELECT EMPLOYEE_ID, FIRST_NAME, LAST_NAME, SALARY, DEPARTMENT_ID FROM EMPLOYEES);

8)CREATE VIEW EMPLOYEES_VU
AS SELECT EMPLOYEE_ID, LAST_NAME AS EMPLOYEE, DEPARTMENT_ID FROM EMPLOYEES ;

9)SELECT * FROM EMPLOYEES_VU;

10)SELECT EMPLOYEE, DEPARTMENT_ID FROM EMPLOYEES_VU
WHERE DEPARTMENT_ID=(SELECT DEPARTMENT_ID FROM DEPARTMENTS WHERE DEPARTMENT_NAME='HR');

11)CREATE VIEW DEPT50
AS SELECT EMPLOYEE_ID AS EMPNO, LAST_NAME AS EMPLOYEE, DEPARTMENT_ID DEPT_NO FROM EMPLOYEES WHERE DEPARTMENT_ID=50 WITH CHECK OPTION CONSTRAINT CHK_DEPT_NO;

12)DESC DEPT50;
SELECT * FROM DEPT50;

13)UPDATE DEPT50 
SET DEPT_NO=80 WHERE EMPLOYEE='Matos';

14)CREATE SEQUENCE DEPT_ID_SEQ
START WITH 200
INCREMENT BY 10
MAXVALUE 1000
NOCYCLE
NOCACHE;

15)INSERT INTO  DEPT(ID, NAME) VALUES(DEPT_ID_SEQ.NEXTVAL, 'EDUCATION'); 
INSERT INTO  DEPT(ID, NAME) VALUES(DEPT_ID_SEQ.NEXTVAL, 'ADMINISTRATION');

16)CREATE INDEX NAME_INDEX 
ON DEPT(NAME);

17)CREATE SYNONYM EMP 
FOR EMPLOYEES;